# Data-Analyzer
Automated ML dataset analyzer that profiles columns, detects data types and targets, measures missing values, cardinality, skew, ranges, outliers, and imbalance, and generates a 0–100 dataset quality score. It ranks ML models with fit scores, explains recommendations, and suggests preprocessing steps.
